def fetch_token():
    # Placeholder for the actual fetch_token logic as described by the user
    # Returning the expected dictionary format
    return {
        "access_token": "your_access_token_here",
        "baseUrl": "your_base_url_here"
    }